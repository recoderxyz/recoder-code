# Recoder OAuth2 Configuration
# These environment variables configure OAuth authentication for Recoder services
# Copy this file to .env and replace placeholder values with actual credentials

# Google OAuth Credentials (for Google AI/Gemini access)
# Get these from Google Cloud Console
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret

# OAuth Base URL - The base URL for Recoder's OAuth service
# Default: https://recoder.xyz
RECODER_OAUTH_BASE_URL=https://recoder.xyz

# OAuth Client ID - The client identifier for your application
# Default: recoder-cli-default
# Contact support@recoder.xyz to obtain your own client ID
RECODER_OAUTH_CLIENT_ID=your-client-id-here

# OAuth Scopes - Space-separated list of permissions
# Default: openid profile email model.completion ai.generate
# Available scopes:
#   - openid: Basic identity access
#   - profile: User profile information
#   - email: User email address
#   - model.completion: Text generation models
#   - ai.generate: AI generation capabilities
#   - code.analyze: Code analysis features
#   - workspace.access: Workspace synchronization
RECODER_OAUTH_SCOPE="openid profile email model.completion ai.generate"

# Development/Testing Configuration
# Uncomment these for local development or testing

# Use localhost for development
# RECODER_OAUTH_BASE_URL=http://localhost:3000

# Development client ID
# RECODER_OAUTH_CLIENT_ID=dev-client-id

# Extended scopes for development
# RECODER_OAUTH_SCOPE="openid profile email model.completion ai.generate code.analyze workspace.access"

# API Keys (Optional - for direct API access)
# These are alternatives to OAuth for certain providers

# OpenRouter API Key (for OpenRouter models)
# Get your key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-key-here

# OpenAI API Key (for direct OpenAI access)
# Get your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-key-here

# Anthropic API Key (for direct Claude access)
# Get your key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-key-here

# Google AI API Key (for direct Gemini access)
# Get your key from https://makersuite.google.com/app/apikey
GOOGLE_AI_API_KEY=your-google-ai-key-here

# Groq API Key (for Groq models)
# Get your key from https://console.groq.com/keys
GROQ_API_KEY=your-groq-key-here

# Configuration Options

# Disable browser launching during OAuth (useful for headless environments)
# RECODER_DISABLE_BROWSER_LAUNCH=true

# Debug mode - enables additional logging
# RECODER_DEBUG=true

# Custom user agent for API requests
# RECODER_USER_AGENT="Recoder CLI/1.0.0"

# Cache directory override (default: ~/.recoder)
# RECODER_CACHE_DIR=/custom/cache/path

# Token refresh buffer time in seconds (default: 300 = 5 minutes)
# RECODER_TOKEN_REFRESH_BUFFER=300

# Network timeout in milliseconds (default: 30000 = 30 seconds)
# RECODER_NETWORK_TIMEOUT=30000

# Local AI Configuration (Optional)

# Ollama endpoint (if running locally)
# OLLAMA_HOST=http://localhost:11434

# LM Studio endpoint (if running locally)
# LM_STUDIO_HOST=http://localhost:1234

# Custom OpenAI-compatible endpoint
# CUSTOM_AI_ENDPOINT=http://localhost:8000/v1
# CUSTOM_AI_API_KEY=your-custom-key-here

# Production Configuration Notes:
# - Never commit this file with real API keys
# - Use environment-specific .env files (.env.production, .env.development)
# - Consider using secret management systems in production
# - Rotate API keys regularly
# - Monitor API usage and costs

# Example Production Setup:
# 1. Copy this file to .env
# 2. Fill in your actual API keys and configuration
# 3. Ensure .env is in your .gitignore
# 4. Use different .env files for different environments
